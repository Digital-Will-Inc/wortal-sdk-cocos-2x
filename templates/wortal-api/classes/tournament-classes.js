/**
 * @typedef {Object} Tournament
 * Represents a tournament in the game.
 * @property {string} id ID of the tournament.
 * @property {string} contextID Context ID the tournament is associated with.
 * @property {number} endTime Unix timestamp of when the tournament ends.
 * @property {string | undefined} title Title of the tournament.
 * @property {object | undefined} payload Optional payload that can be used to store custom data.
 */
